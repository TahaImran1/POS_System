# Changes Summary - 2026-08-21

- Fixed `SQLITE_CONSTRAINT_UNIQUE: UNIQUE constraint failed: users.username` error during Initial Setup Wizard:
  - Updated `addUser()` in `useAuthStore.ts` to perform graceful upsert: if an account with the specified username already exists, it updates the existing account's credentials (PIN, name, role, terminal) instead of throwing an SQLite UNIQUE constraint violation.
  - Updated `InitialNodeSetupWizard.vue` step 6 to reload users before setup completion and correctly set authenticated session state.
  - Removed intrusive blocking `alert()` modal on database query errors in `src/db/client.ts`.
