# System Architecture & Requirements Update - 2026-08-01

- Documented the new **Universal Edge-Node Hybrid Architecture** in `doc/System_Architecture.md`, defining a single codebase capable of operating as Standalone POS, Leaf POS, Branch Server, or Enterprise HQ Server.
- Detailed the **Outbound Client-Initiated Sync Protocol (PULL & PUSH)** to eliminate the requirement for static local IPs on POS terminal devices.
- Formulated the **Dynamic Hierarchy & Failover Bypass Routing** mechanism allowing child POS/branch nodes to automatically fallback directly to Grandparent/HQ servers if an intermediate server is offline or removed.
- Specified the **Data Integrity & Collision Prevention Strategy** utilizing UUIDv7 primary keys combined with composite tracking metadata (`branch_id`, `pos_id`, `user_id`, `updated_at`).
- Defined the **Heterogeneous Database Transport Abstraction** decoupling edge SQLite databases from server PostgreSQL/Oracle databases using JSON DTO sync payloads over HTTP/WebSocket APIs.
- Outlined the **Parallel Microservices Integration Bridge** for client-specific modules (such as FBR Invoicing in Pakistan), enabling external apps in Python, C#, or .NET to run in parallel and consume sales events via webhooks.
- Designed an **Offline Anti-Piracy & Licensing Engine** using hardware machine fingerprinting, asymmetric Ed25519 cryptographic `.lic` signatures, anti-clock rewinding protection, and subscription grace period lifecycle management without third-party SaaS dependencies.
- Enforced **Financial Transaction Integrity Controls** including immutable double-entry ledger modeling, idempotent payload sync keys (`sync_id`), and strict ACID transaction boundaries.
- Created the **System Diagnostics Package Exporter Specification** for one-click snapshot export to recreate client-specific bugs in local dev environments.
- Converted all Mermaid diagram blocks in `doc/System_Architecture.md` into clean ASCII text arrow diagrams for easier readability and review.
- Updated the synchronization architecture specification in `doc/System_Architecture.md` to use **Direct Database Access Sync (DBeaver Style)** via bundled remote database drivers, removing custom HTTP Web API middle layers and solving Mikrotik/Multi-WAN network routing challenges.
- Specified the **Tamper-Proof Security & Database Permissions Model**, enforcing PostgreSQL least privilege roles (`GRANT SELECT, INSERT / REVOKE UPDATE, DELETE`), UI read-only immutability for finalized receipts, and SHA256 cryptographic hash chain verification to prevent data corruption.
- Formulated the **Controlled On-Duty Sales Modification & Audit Lineage Engine** in `doc/System_Architecture.md`, allowing on-duty sales personnel with active shift sessions to edit/void sales while transactionally logging `modified_by_user_id`, `shift_session_id`, action type, reason notes, and before/after JSON snapshots into `sales_audit_logs`.
- Established the **Electron Desktop Runtime Framework** in `doc/System_Architecture.md`, packaging Vue 3 + Vite in a dedicated desktop container with native Node.js Main process execution (`better-sqlite3`, `pg`), frameless kiosk mode support, and direct ESC/POS hardware peripheral access.
- Implemented **Phase 1 Universal Edge-Node System Architecture** in `f:\Projects\possystem\pos_app`:
  - Created `src/types/nodeConfig.ts` with `NodeRole` definitions (`STANDALONE_POS`, `LEAF_POS`, `BRANCH_SERVER`, `ENTERPRISE_HQ`) and upstream DB connection settings.
  - Created `src/stores/useNodeConfigStore.ts` Pinia store for persistent node role switching and connection management.
  - Updated `src/db/schema.ts` with origin tracking metadata (`origin_branch_id`, `origin_pos_id`, `origin_user_id`, `synced`) and `sales_audit_logs` table schema for tracking on-duty sales edits.
  - Created `src/services/licenseService.ts` for device fingerprinting and offline license verification.
  - Created `src/services/directDbSyncService.ts` for executing direct PostgreSQL DB pull/push sync cycles with failover fallback.
  - Created `src/views/NodeConfigView.vue` UI for node configuration, license diagnostic overview, and manual sync execution.
  - Integrated `NodeConfigView` into top navigation in `src/App.vue`.
  - Updated `src/components/setup/InitialNodeSetupWizard.vue` to adopt official **Odoo POS design aesthetics** (`#714B67` primary header, warm gold `#E0A96D`, Odoo cards).
  - Fixed `ERR_FILE_NOT_FOUND` in Electron by adding a `window.location.protocol === 'file:'` check in `src/App.vue` to prevent invalid `window.history.pushState` file path overrides under Electron file protocol.
  - Enhanced `src/components/setup/InitialNodeSetupWizard.vue` Step 2 with explicit database configuration controls: Database File Name input (`pos_local.sqlite`), Initial Seeding Mode toggle (`Seed Initial Odoo Catalog & Demo Accounts`), and clear architectural explanation of serverless SQLite file storage.
  - Added `db_storage_path` configuration setting to `src/types/nodeConfig.ts`, `src/stores/useNodeConfigStore.ts`, and `src/components/setup/InitialNodeSetupWizard.vue` Step 2, allowing store owners to specify a custom database storage/backup directory (e.g. `F:\Backup\POS_Data` or `D:\POS_Data`) to protect data against C: drive Windows corruption.
  - Integrated native Windows folder selection dialog (`dialog:openDirectory` IPC handle in `electron/main.cjs` and `electron/preload.cjs`), exposing a **"Browse..."** folder button in `InitialNodeSetupWizard.vue` Step 2 so users can visually select their database directory.
  - Fixed `dialog:openDirectory` registration timing in `electron/main.cjs` to register IPC handlers prior to window creation.
  - Added auto-detection in `electron/main.cjs` to automatically connect to Vite dev server (`http://localhost:5173`) when active, enabling instant **Live HMR Reloading** without needing to restart Electron when editing `.vue` or `.ts` files. Added `"electron:dev"` script to `package.json`.
  - Updated `"electron:dev"` script in `package.json` to use cross-platform `concurrently` and `wait-on` (`"electron:dev": "npx -y concurrently \"vite\" \"npx -y wait-on http://localhost:5173 && electron .\""`), ensuring both Vite dev server and Electron boot up in parallel on Windows PowerShell with instant Live HMR Reloading.
  - Added `pg` and `@types/pg` dependencies to `package.json`.















