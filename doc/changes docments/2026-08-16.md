# Changes Summary - 2026-08-16

- Set up automated Git Release workflow for `pos_app` (Electron POS Client).
- Created `.github/workflows/release.yml` with `permissions: contents: write` and `GITHUB_TOKEN` support for private repository builds.
- Updated `package.json` with repository metadata (`git+https://github.com/TahaImran1/POS_System.git`) and `electron-builder` GitHub publish provider setting.
- Updated `publish` config to standard public GitHub provider for direct client auto-update checks.
- Updated `.github/workflows/release.yml` to use Node 22 (satisfying SQLite WASM engine requirements) and switched to `npm install` to avoid lockfile mismatch aborts.
