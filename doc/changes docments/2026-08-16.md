# Changes Summary - 2026-08-16

- Set up automated Git Release workflow for `pos_app` (Electron POS Client).
- Created `.github/workflows/release.yml` with `permissions: contents: write` and `GITHUB_TOKEN` support for private repository builds.
- Updated `package.json` with repository metadata (`git+https://github.com/TahaImran1/POS_System.git`) and `electron-builder` GitHub publish provider setting.
- Updated `publish` config to standard public GitHub provider for direct client auto-update checks.
- Enforced strict PIN-only authentication for user switching:
  - Removed unauthenticated role switch shortcuts in `LoginModal.vue` and `useAuthStore.ts`.
  - Removed plain-text PIN exposure from the user switch modal.
  - Redesigned `LoginModal.vue` with user account selection chips, masked PIN display, touch/mouse numpad, and physical keyboard support.
- Enforced startup PIN login requirement when app setup is completed:
  - Removed default/automatic developer role login in `useAuthStore.ts`.
  - Configured app to start in locked unauthenticated state (`isAuthenticated: false`) and automatically present mandatory PIN lock screen on launch.
- Added "Staff & Salespersons" management tab and modal to `ManagerPortalView.vue`:
  - Enabled Store Managers to view all staff accounts, add new salespersons/cashiers with custom/auto-generated PINs, edit existing accounts, and delete inactive staff.
- Added PDF Document Preview Modal before printing reports in `ManagerPortalView.vue`:
  - Replaced direct browser print trigger with an A4 official accounting document preview modal.
  - Formatted preview with store branding, date filters, KPI summary, itemized tax audit tables, tax breakdown calculations, and signature sections.
  - Implemented `@media print` CSS isolating only the clean document sheet when the user clicks "Print Report".
