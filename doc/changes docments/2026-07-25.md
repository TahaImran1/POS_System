# Changes - 2026-07-25

- **Settings & Modes Feature**:
  - Created `useSettingsStore.ts` to manage application mode (`retail` | `restaurant`) and store metadata (storeName: `pyora`).
  - Added `SettingsModal.vue` allowing instant switching between Retail and Restaurant POS modes.
  - Added a dedicated **Settings** tab in the top header and a prominent Settings button with inline SVG in `POSLayout.vue`.
  - Added `TablePromptModal.vue` to prompt for Table Numbers when starting new orders in Restaurant mode.

- **Payment Success & Receipt Modal**:
  - Created `ReceiptModal.vue` to display transaction summaries, change due for cash payments, and mock printed receipts.
  - Updated `CheckoutModal.vue` and `useCartStore.ts` to return receipt payload upon order validation.

- **Empty State Polish**:
  - Implemented an empty state graphic in `POSLayout.vue` when no active tickets exist.

- **UI & Icon Fixes**:
  - Replaced FontAwesome dependencies with inline SVGs for Close, Delete, and Settings buttons to ensure rendering reliability across environments.

- **Odoo Restaurant POS Live Clone (1:1)**:
  - Inspected live Odoo Restaurant instance (`https://edu-pyora.odoo.com/pos/ui/2/floor`) using browser subagent.
  - Rebuilt top navbar matching Odoo structure: `Tables` | `Register` | `Orders` (with dynamic active orders badge) and active sale badge (`Direct Sale` / `Table 1` / `Table 2`).
  - Created `FloorPlanView.vue` with floor subheader (`+ New Order`, `pyora` restaurant name, `Get QR Codes`, `#` button) and interactive grid table cards.
  - Created `OrdersView.vue` with search, `Active`/`Ongoing`/`Paid` status filters, order rows displaying pink table badges (`Table 1`), itemized receipt preview, tax & total breakdown, and large purple `Load Order` button (`#714B67`).
  - Created `CustomerModal.vue` for customer search, selection, and creation (with name, phone, email, city).
  - Created `TransferModal.vue` for table transfers.
  - Created `ActionsModal.vue` replicating Odoo's exact 3x3 Grid Actions modal (Customer Note, Guests, Transfer / Merge, Transfer course, Edit Order Name, Quotation/Order, Pricelist, Refund, Cancel Order).
  - Created `HeaderMenuModal.vue` connected to the top-right hamburger menu (`☰`) for Cash In / Cash Out, Close Session, Orders, and Settings.
  - Created `QuotationModal.vue` for creating backend Sales Quotations.
  - Created `PricelistModal.vue` for selecting customer pricelists (Standard, VIP 10%, Wholesale).
  - Created `ProductInfoModal.vue` for Odoo product detail modal views matching `/product/<id>`.
  - Added `#` Floor Layout Editor side panel drawer in `FloorPlanView.vue` for adding tables and configuring seat counts.
  - Implemented URL route synchronization (`/floor`, `/register`, `/ticket`) in `POSLayout.vue`.
  - Implemented Collapsible Numpad behavior in `LeftPane.vue` & `Numpad.vue` (collapses on empty cart, expands when items exist).
  - Updated `Numpad.vue` with Odoo action row (`Customer`, `Note`, `⬆ Upload/Course`, `Course`, `: Actions`), `%` button, `⌫` backspace label, and dynamic bottom row (`Set Table`, `Set Tab`, `Payment` `#563550` or `Fire Course 1` `#563550` & `Payment`).
  - Grouped Cart lines by Course Banners (`Course 1`, `Course 2`) matching exact Odoo green banner styling (`#A7F3D0`).
  - Updated `ProductCard.vue` to feature grey image containers, category color bottom bars, and green rounded badge counters `1` on top-right matching live Odoo.
  - Updated `RightPane.vue` rectangular category pills with exact colors (`Misc` pink, `Desks` pink, `Chairs` teal).
  - Seeded exact live Odoo products (*Office Chair*, *Desk Combination*, *Storage Box*, *Virtual Interior Design*, etc.) in `src/db/seed.ts` and `useProductStore.ts`.
