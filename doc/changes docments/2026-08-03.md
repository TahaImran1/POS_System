# Changes - 2026-08-03

- **Phase 1 Setup Flow Implementation for POS App (`pos_app`)**:
  - Implemented the exact Phase 1 onboarding sequence in `pos_app/src/components/setup/InitialNodeSetupWizard.vue`:
    1. **Database Setup / Initialization**: Connected local SQLite/IndexedDB database initialization badge.
    2. **Step 1: Choose POS System Type**: Visual selection screen asking user for system type:
       - 🍽️ **Restaurant POS**: Table canvas floor plan, Kitchen Order Tickets (KOT/KDS), BOM recipe ingredient stock deductions.
       - 🛒 **Store / Retail POS**: Fast barcode scanner checkout, direct item grid, inventory stocking.
    3. **Step 2: Initialize Manager Credentials**: Setup Store Name, Manager Username, Full Name, and Password/PIN.
    4. **Step 3: Complete & Launch Workspace**: Saves selected POS mode & store name in `useSettingsStore.ts`, inserts Manager user into database via `useAuthStore.ts`, and transitions directly into the selected POS workspace.

- **Database Location Explanation & Manual Export Controls**:
  - Updated `DeveloperPortalView.vue` with an informative banner explaining that browser Web Mode uses SQLite WASM **OPFS (Origin Private File System)** at `/pos.sqlite`.
  - Added **"Save Copy to Disk"** button to export the physical `.sqlite` database file directly to `F:\tempdb` or local downloads.
  - Added **"Delete DB & Reset Setup"** red action button to purge OPFS and reset Phase 1 setup cleanly.

- **Dynamic POS Terminals Catalog & Dashboard Cards**:
  - Updated `POSDashboardView.vue` and `useSettingsStore.ts` to dynamically render user-created POS terminals instead of static hardcoded cards.
  - Added **`+ Create New POS Terminal`** modal right on the POS Dashboard header bar to allow creating new POS outlets dynamically anytime with persistent `localStorage` saving!

- **Session Initialization & Demo Users Seeding Fixes**:
  - Fixed `useSessionStore.ts` to default to `isOpen: false` and `openingBalance: 0` for newly created terminals (removed hardcoded `Mitchell Admin` and `1000` opening float).
  - Fixed `src/db/seed.ts` to remove hardcoded demo users (`manager: 5555`, `cashier: 0000`) so clean setup saves ONLY the user's initialized Manager account.
  - Fixed ticket calculation in `POSDashboardView.vue` so empty carts do not count as ongoing orders.
