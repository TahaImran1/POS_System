# Changes - 2026-07-29

- **Odoo POS Master Backend Architecture & Ubuntu Server Deployment Suite**:
  - Structured production-grade Master Backend server ready for GitHub repository `https://github.com/TahaImran1/pos-backend.git`:
    - **`package.json`**: Configured Node.js ES Module dependencies (`fastify`, `@fastify/cors`, `@fastify/jwt`, `@fastify/websocket`, `pg`, `dotenv`).
    - **`tsconfig.json`**: Configured TypeScript NodeNext compilation settings.
    - **`src/index.ts`**: Complete Master Backend entry point registering modular Fastify plugins (`auth`, `products`, `sessions`, `orders`, `sync`, `admin`).
    - **`Dockerfile`**: Multi-stage Docker container build definition (`npm install` & `npm install --omit=dev`).
    - **`docker-compose.yml`**: Docker Compose configuration for `pos-db` PostgreSQL container, `pos-backend` app container, and `pos-nginx` reverse proxy container.
    - **`nginx/nginx.conf`**: Nginx configuration with CORS, Gzip, and WebSocket upgrade headers (`Upgrade $http_upgrade`, `Connection "Upgrade"`).
    - **`.env.example` & `.gitignore`**: Environment variables template and git ignore rules excluding secrets and database data volumes.
    - **`README.md`**: Complete Ubuntu Server deployment guide and API documentation for `https://github.com/TahaImran1/pos-backend.git`.

- **PostgreSQL 15 Master Database Integration & Port Conflict Resolution**:
  - Switched Master Database engine from SQLite to **PostgreSQL 15** (`postgres:15-alpine` container named `odoo_pos_postgres_db`).
  - Bound PostgreSQL host port to **`5433`** (`POS_DB_PORT=5433`), guaranteeing zero port conflict with PharmaApp's PostgreSQL on port `5432`.
  - Created `src/db/db.ts` and `src/db/schema.sql` with full DDL tables (`nodes`, `users`, `product_categories`, `products`, `product_bom`, `stock_quant`, `partners`, `cash_sessions`, `cash_statements`, `pos_orders`, `pos_order_lines`, `sync_events`).

- **Multi-Tenant SaaS Client Architecture & Dynamic Schema Engine (`<client_name>_pos`)**:
  - Created `pos_backend/src/db/tenant.ts` with `createClientSchema(companyName, posType)` helper function:
    - Slugifies company names (e.g. *"KFC Pakistan"* -> `kfc_pakistan_pos`).
    - Provisions dedicated database schemas with full **Header-Line Relational Tables** (`branches`, `users`, `product_categories`, `products`, `product_bom`, `product_bom_lines`, `stock_quant`, `cash_sessions`, `cash_statements`, `pos_orders`, `pos_order_lines`, `sync_events`).
    - Provisions **Odoo Tax Engine Tables** (`account_tax` for general tax rules & `product_taxes_rel` for many-to-many product-tax association).
  - Updated `public.client_tenants` and `public.master_users` SaaS routing tables.
  - Implemented Super Admin APIs (`/api/admin/clients`, `/api/admin/client-managers`) and schema-scoped JWT authentication.

- **Admin & Manager Control Center App (`pos_dashboard`) Implementation**:
  - Built React 19 + Next.js 15 + TypeScript + TailwindCSS web application matching `F:\Projects\PharmaApp\admin-dashboard` tech stack:
    - **Super Admin Features**: Provision Client Companies (Company Name e.g. "KFC Pakistan", POS Type `RESTAURANT` vs `RETAIL`, Max Branches), display generated PostgreSQL schema slug (`kfc_pakistan_pos`), and assign Client Manager accounts.
    - **Client Manager Features**: Dedicated company view locked to `<client_name>_pos` schema. Manage Branch Outlets, General Tax Rules (`/taxes`), Product Catalogs & BOM Recipes, Cash Session Audits, and Sales Reports.
  - Added Docker container orchestration (`Dockerfile` & `docker-compose.yml`) running on port **`8090`** (`POS_DASHBOARD_PORT=8090`).
