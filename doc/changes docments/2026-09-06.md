# Changes Documentation - 2026-09-06

## Complete Offline Setup Installer Packaging & Dependency Bundling

- **Offline Asset & Dependency Bundling**:
  - Installed `@fortawesome/fontawesome-free` as a local dependency and imported `@fortawesome/fontawesome-free/css/all.min.css` in `src/main.ts`.
  - Removed external CDN dependency link (`cdnjs.cloudflare.com`) from `index.html`, bundling all Font Awesome webfonts (`.woff2`) directly into `dist/assets/`.
  - Verified local bundling of all runtime assets including `@sqlite.org/sqlite-wasm` binaries and workers, Vue 3, Pinia, and Tailwind CSS.

- **Standalone Windows Setup Installer Compilation (`release/`)**:
  - Executed full production build and packaging with `electron-builder` (`npx.cmd electron-builder --win`):
    - Generated standalone NSIS Windows Installer: [`release/POS Enterprise System Setup 1.0.3.exe`](file:///c:/pos%20system/release/POS%20Enterprise%20System%20Setup%201.0.3.exe) (96.4 MB).
    - Generated unpacked directory distribution: `release/win-unpacked/` with `POS Enterprise System.exe`.
    - Includes automatic desktop shortcut, start menu shortcut, uninstaller, and offline database persistence.

## Developer Mode & Immutable Installed Store Profile

- **Locked Settings Modal (`src/components/modals/SettingsModal.vue`)**:
  - Removed the `DEV SWITCH` toggle buttons that previously allowed cashiers or developers to switch between Retail and Restaurant modes.
  - Locked the **Store Name** input to read-only with a padlock indicator (`🔒 Locked (Configured at Setup)`).
  - Locked the **Configured POS Type** display to read-only (`🔒 Permanent`) showing active operational mode (`🛒 Store / Retail POS` or `🍽️ Restaurant POS`).
  - Added a prominent protection banner explaining that the node identity is declared once at setup and cannot be switched in the installed application to guarantee catalog, tax, and sales transaction integrity.

- **POS Layout Header Protection (`src/components/POSLayout.vue`)**:
  - Removed the `[DEV SWITCH]` tag from the register top bar next to the mode badge.

- **POS Dashboard Synchronization (`src/views/POSDashboardView.vue`)**:
  - Updated `handleOpenRegisterClick()` and active terminal cards to strictly inherit the declared `settingsStore.storeName` and `settingsStore.posMode`.
  - Removed terminal creation and switching actions that allowed creating conflicting terminal modes within the installed app.
  - Added a locked terminal status badge (`Installed Terminal Configuration Locked`).

- **Developer Portal Identity Audit (`src/views/DeveloperPortalView.vue`)**:
  - Added an **Installed Terminal Identity (Immutable)** card in the Diagnostics tab displaying the permanent store name and operating mode with a locked status badge.

- **Verification**:
  - Ran `npm.cmd run build` (`vue-tsc -b && vite build`): Succeeded with zero TypeScript or template errors.
  - Verified package output in `release/`: `POS Enterprise System Setup 1.0.3.exe` (96,393,111 bytes).

## Git Release & Distribution Packaging (v1.0.4)

- **Version Bump**:
  - Incremented version from `1.0.3` to `1.0.4` across `package.json` and `package-lock.json` to prevent collision with previously published git tags and releases.
- **Installer Compilation**:
  - Built fresh standalone Windows NSIS installer: `release/POS Enterprise System Setup 1.0.4.exe` (96,393,098 bytes).
  - Updated auto-updater manifests: `release/latest.yml` and `release/POS Enterprise System Setup 1.0.4.exe.blockmap`.
- **Git Branch & Tagging**:
  - Verified changes staged exclusively on `dev` branch to adhere to repository rule prohibiting pushes to `main`.
  - Created annotated git release tag `v1.0.4` pointing to the release commit on `dev`.
  - Pushed `dev` branch and `v1.0.4` release tag to remote GitHub repository (`origin`).
  - Triggered GitHub Actions automated release pipeline (`.github/workflows/release.yml`) to compile and publish binary assets to GitHub Releases.

